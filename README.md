# MoodTracker++

Демонстрационное приложение для изучения различных подходов к персистентности во Flutter.

## Описание

MoodTracker++ - это приложение для отслеживания настроения, которое демонстрирует различные библиотеки и подходы к сохранению данных во Flutter. Каждая ветка представляет отдельный подход к персистентности.

## Структура веток

### 00_initial - Базовая версия без персистентности
- Хранение данных только в памяти
- Потеря данных при перезапуске приложения
- Основа для всех последующих реализаций
- **Используемые пакеты:**
  - `provider` - управление состоянием
  - `intl` - форматирование дат

### 01_shared_prefs - SharedPreferences ✅
- Простое key-value хранилище для настроек
- Сохранение темы приложения (светлая/темная)
- Сохранение состояния аутентификации
- Счетчик запусков приложения
- Напоминания для ежедневных записей
- **Используемые пакеты:**
  - `shared_preferences: ^2.3.2` - key-value хранилище
  - Все пакеты из 00_initial

### 02_secure_storage - Secure Storage ✅
- Шифрованное хранилище для чувствительных данных
- Интеграция с Keychain (iOS) и Keystore (Android)
- Хранение токенов аутентификации и паролей
- Firebase Authentication интеграция
- **Используемые пакеты:**
  - `flutter_secure_storage: ^9.2.2` - шифрованное хранилище
  - `firebase_core: ^3.8.0` - Firebase SDK
  - `firebase_auth: ^5.3.4` - аутентификация
  - Все пакеты из предыдущих веток

### 03_files - Файловая система
- *(Ветка будет добавлена)*
- Работа с локальными файлами
- Экспорт/импорт данных в JSON и CSV

### 04_nosql - NoSQL базы данных
- *(Ветка будет добавлена)*
- Реализация с ObjectBox и Realm
- Высокопроизводительные встраиваемые базы данных

### 05_sql - SQL база данных
- *(Ветка будет добавлена)*
- SQLite через Drift
- Реляционная база данных с миграциями

### 06_cloud_storage - Облачное хранилище
- *(Ветка будет добавлена)*
- Firebase Storage и Firestore
- Синхронизация данных между устройствами

### 07_clean_data - Очистка данных
- *(Ветка будет добавлена)*
- Полная очистка всех типов хранилищ
- Безопасный выход из аккаунта

## Функционал приложения

- 📝 Запись настроения с эмодзи и цветовой индикацией
- 📊 Добавление заметок к записям
- 🏃 Отметка активностей
- 💤 Отслеживание сна
- 📷 Прикрепление фотографий
- 📈 История записей
- 📊 Статистика настроений
- 🌙 Темная тема
- ☁️ Облачная синхронизация
- 🔐 Безопасное хранение данных

## Структура проекта

```
lib/
├── main.dart              # Точка входа
├── models/               # Модели данных
│   └── mood_entry.dart
├── screens/              # Экраны приложения
│   ├── home_screen.dart
│   ├── add_mood_screen.dart
│   ├── mood_history_screen.dart
│   ├── statistics_screen.dart
│   ├── profile_screen.dart
│   └── login_screen.dart
├── services/             # Сервисы
│   ├── mood_service.dart
│   ├── theme_service.dart
│   ├── auth_service.dart
│   └── preferences_service.dart
└── widgets/              # Переиспользуемые виджеты
```

## Ветка 01_shared_prefs

В этой ветке добавлена персистентность настроек приложения через SharedPreferences:

### Реализованные функции:
- 🔐 **Аутентификация** - сохранение состояния входа и имени пользователя
- 🌓 **Тема приложения** - сохранение выбора темной/светлой темы
- 📊 **Статистика использования** - счетчик запусков приложения
- ⏰ **Напоминания** - настраиваемые ежедневные напоминания

### Технические детали:
- Пароль для входа: `password`
- Данные сохраняются в SharedPreferences при изменении
- Автоматическая загрузка настроек при запуске
- Профиль пользователя с индикатором авторизации

**Примечание:** Записи настроения по-прежнему хранятся только в памяти и теряются при перезапуске.

## Ветка 02_secure_storage

В этой ветке добавлено безопасное хранение чувствительных данных через flutter_secure_storage и интеграция с Firebase Authentication:

### Реализованные функции:
- 🔐 **Firebase Authentication** - регистрация и вход через email/пароль
- 🔑 **Безопасное хранение токенов** - Firebase токены в зашифрованном хранилище
- 🛡️ **Шифрование паролей** - локальное хранение зашифрованных учетных данных
- 📱 **Автоматический вход** - безопасное сохранение сессии
- 🚪 **Безопасный выход** - очистка токенов и сессии

### Технические детали:
- Использует Keychain (iOS) и Keystore (Android) для шифрования
- Firebase проект требуется для работы аутентификации
- Автоматическая проверка токена при запуске
- Обработка ошибок сети и аутентификации
- Secure Storage для хранения:
  - Firebase ID токенов
  - Refresh токенов
  - Email пользователя
  - Времени последнего входа

### Настройка Firebase:
1. Создайте проект в Firebase Console
2. Добавьте приложение для Android/iOS
3. Скачайте и добавьте конфигурационные файлы:
   - `android/app/google-services.json`
   - `ios/Runner/GoogleService-Info.plist`
4. Включите Email/Password аутентификацию

**Примечание:** Записи настроения по-прежнему хранятся только в памяти. SharedPreferences используется для настроек, Secure Storage - для токенов.

## Запуск проекта

```bash
# Клонирование репозитория
git clone https://github.com/dzolotov/moodtracker.git
cd moodtracker

# Переключение на нужную ветку
git checkout 02_secure_storage

# Установка зависимостей
flutter pub get

# Запуск приложения
flutter run
```

## Требования

- Flutter 3.7.2 или выше
- Dart SDK 3.7.2 или выше
- Для некоторых веток:
  - Firebase проект (для веток с Firebase)
  - Android Studio / Xcode (для нативных функций)

## Использование

Каждая ветка содержит README с подробным описанием реализованного функционала и используемых технологий. Переключайтесь между ветками для изучения различных подходов к персистентности.

## Тестирование

Проект содержит юнит-тесты для основных компонентов. Покрытие тестами варьируется в зависимости от ветки:

| Ветка | Тестовое покрытие | Примечание |
|-------|-------------------|------------|
| **00_initial** | 98.7% (379 из 384 строк) | Полное покрытие всех компонентов |
| **01_shared_prefs** | 99.2% (628 из 633 строк) | Добавлены тесты для новых экранов |
| **02_secure_storage** | 58.0% (572 из 986 строк)* | *Улучшено с помощью DI и platform channel моков |

### Запуск тестов

```bash
# Запуск всех тестов
flutter test

# Запуск тестов с отчетом о покрытии
flutter test --coverage

# Генерация HTML отчета
genhtml coverage/lcov.info -o coverage/html
open coverage/html/index.html
```

### Примечание для ветки 02_secure_storage

Покрытие тестами улучшено с 30.2% до 58.0% благодаря использованию паттерна Dependency Injection и мокированию на уровне platform channels.

Протестированы:
- Модели данных (MoodEntry) - 100%
- Базовые сервисы (MoodService, ThemeService, PreferencesService) - 100%
- AuthServiceInjectable с полным покрытием всех методов - 83%
- **AuthServiceDI** с comprehensive тестированием - 57.2%
- Platform channel тесты для Firebase Auth
- Юнит-тесты для SecureStorageService с моками
- Экраны авторизации:
  - LoginScreen - 78%
  - ProfileScreen - 54%
  - PasswordResetScreen - 58%
  - EditProfileScreen - 42%

Подходы к тестированию Firebase-зависимых компонентов:
- **Dependency Injection** - создан AuthServiceDI для инжекции mock-объектов
- **Firebase Mocking** - полное мокирование Firebase Core и Auth через platform channels
- **Secure Storage Mocking** - полная имитация flutter_secure_storage
- **Test Helpers** - утилиты для создания тестового окружения с провайдерами

Новые тестовые компоненты:
- `test/mock_firebase.dart` - комплексная система мокирования Firebase
- `test/services/auth_service_di_test.dart` - 19 групп тестов с 23 тестовыми случаями
- `lib/services/auth_service_di.dart` - версия AuthService с полной поддержкой DI

## Лицензия

MIT License